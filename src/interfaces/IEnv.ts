export interface IEnv {
  DB_HOST: string;
  DB_PORT: number;
  DB_PASSWORD: string;
  DB_USERNAME: string;
  DB_DATABASE: string;
  DEFAULT_USER_LOGIN: string;
  DEFAULT_USER_PASSWORD: string;
  DEFAULT_USER_EMAIL: string;
}
